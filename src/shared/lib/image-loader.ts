/**
 * next/image по умолчанию либо читает src как собственный файл фронтенда (относительный
 * путь), либо ходит за ним по адресу из `images.remotePatterns`. Ни то, ни другое не подходит
 * нашим картинкам: `assetUrl()` (shared/api/base.ts) отдаёт то относительный путь (прод — тот
 * же origin через nginx), то абсолютный (дев — localhost) в зависимости от окружения, а сам
 * файл в обоих случаях физически лежит на бэкенде, а не во фронтенд-контейнере — прод-сборка
 * next/image приняла бы относительный /storage/... за свой собственный и не нашла бы файл.
 * Плюс бэкенд не отдаёт уменьшенные варианты — оптимизировать всё равно нечего. Loader просто
 * возвращает src как есть — браузер идёт за картинкой напрямую, как раньше делал <img src>.
 */
export default function imageLoader({ src }: { src: string }) {
  return src;
}
